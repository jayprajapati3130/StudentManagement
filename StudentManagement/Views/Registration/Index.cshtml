@model IEnumerable<StudentManagement.Models.registration>

@{
    ViewBag.Title = "Index";
}

<div style="background-color:aquamarineS">
    <h2>Students List</h2>

    @*<p>
        @Html.ActionLink("Create New", "Create")*@
    @*<a href="@Url.Action("Create", "Registration","",htmlattribute)">
            <img src="~/Image/addIcon.png" class="img-responsive" style="width:min-content; height:51px" />
        </a>*@
    @* </p>*@

    @Html.ActionLink("Add", "Create", "Registration", new { @class = "btn btn-primary" })


    @* <p>
            @using (Html.BeginForm("Index", "Registration", FormMethod.Get))
            {
                <b> Search by</b> @Html.RadioButton("searchby", "firstname", true)<b> FirstName</b>
                @Html.RadioButton("searchby", "course_id") <b>Course Id</b><br />
                @Html.TextBox("search") <input type="submit" value=" Search" />
            }
        </p>*@

    <br />
    <table class="table" id="myTable">
        <thead>
            <tr style="background-color:#080a45 ; color:white">
                <th>
                    @Html.DisplayNameFor(model => model.firstname)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.lastname)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.telno)
                </th>
                <th>
                    @* @Html.DisplayNameFor(model => model.batch.batch1)*@
                    Batch
                </th>
                <th>
                    @* @Html.DisplayNameFor(model => model.course.course1)*@
                    Course
                </th>
                <th>
                    Action
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr style="background-color: #ffb3ff">
                    <td>
                        @Html.DisplayFor(modelItem => item.firstname)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.lastname)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.telno)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.batch.batch1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.course.course1)
                    </td>
                    <td>
                        <button type="button" style="background-color:forestgreen"> @Html.ActionLink("Edit", "Edit", new { id = item.id }, new { @style = "color:#FFFFFF;" })</button>
                        <button type="button" style="background-color:forestgreen"> @Html.ActionLink("Info", "Details", new { id = item.id }, new { @style = "color:#FFFFFF;" })</button>

                        @* @Html.ActionLink("Details", "Details", new { id=item.id }) |*@
                        @* @Html.ActionLink("Delete", "Delete", new { id=item.id }, new { @style = "color:#FF0000;" })*@

                        <button type="button" style="background-color:orangered">@Html.ActionLink("Delete", "Delete", new { id = item.id }, new { @style = "color:#FFFFFF;" })</button>


                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div>
        @Html.ActionLink("Export PDF", "PrintReport", "Registration", new { @class = "btn btn-primary" })
    </div>




</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('#myTable').DataTable();
    });

</script>
